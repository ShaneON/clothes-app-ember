<div local-class="item">
  {{#link-to 'gallery.image' image class=(local-class "link")}}
    <img local-class="image" src="{{url}}">
    <h1 local-class="name">
      {{name}}
    </h1>
  {{/link-to}}
</div>
